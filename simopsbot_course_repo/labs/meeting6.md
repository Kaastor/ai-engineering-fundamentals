# Meeting 6 — Evaluation: Spec + Offline Suite + Regression Gate

## Goal

Turn a demo into an engineering artifact:
- a concrete spec (`spec.md`)
- an offline evaluation suite
- regression thresholds

## Run

```bash
python -m scripts.eval_runner --profile week5 --seeds 0:50 --out outputs/eval
```
